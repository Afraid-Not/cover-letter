# Cover Letter Generator - Project Plan

## 한줄 요약

자소서 질문 + 채용공고 + 본인 이력서를 입력하면, 합격 자소서 기반 RAG로 **사람이 쓴 것처럼 자연스러운 자소서 답변**을 생성해주는 CLI 도구.

---

## 어떤 문제를 풀려고 하나?

- 자소서를 어떤 톤과 구조로 써야 할지 모르겠다
- 채용공고가 원하는 역량에 맞춰서 내 경험을 녹이고 싶다
- 합격한 사람들은 어떻게 썼는지 참고하고 싶다
- AI가 쓴 티가 나면 안 된다

**이 네 가지를 한번에 해결하는 도구.**

---

## 동작 흐름

```
[사전 준비]
합격 자소서 data.txt → 질문-답변 단위 청킹 → 임베딩 → Supabase(벡터DB)에 저장

[사용 시]
1. 자소서 질문 입력
   예: "팀워크를 발휘해 공동 목표 달성에 기여한 경험을 서술하세요"

2. 채용공고 텍스트 입력 (복붙)
   → LLM이 요구 역량/키워드 추출

3. 본인 이력서 입력 (복붙 / txt / md / PDF) 또는 저장된 이력서 선택
   → 최초 입력 시: 파싱 → 구조화 → Supabase에 저장
   → 재사용 시: 저장된 이력서 목록에서 선택

4. RAG 검색
   → 자소서 질문 유형 + 요구 역량과 유사한 합격 자소서 답변들을 벡터DB에서 검색

5. 자소서 답변 생성
   → 합격 자소서 스타일 + 채용공고 요구사항 + 내 이력서 정보
   → LLM이 사람이 쓴 것처럼 자연스러운 답변 작성

6. LLM-as-a-Judge 평가 (9명 병렬)
   → 채용공고 기반으로 페르소나 백그라운드 동적 생성
   → 3그룹 × 3명 = 9명이 독립 평가 (asyncio 병렬 호출)
   → 그룹별 평균 + 종합 서류 통과 확률

7. (선택) 재생성
   → 점수가 낮거나 사용자가 원하면
   → 9명의 피드백을 종합 반영하여 개선된 버전 재생성
```

---

## 기술 스택

| 구분     | 기술                          | 왜?                                  |
| -------- | ----------------------------- | ------------------------------------ |
| 언어     | Python 3.11+                  | LLM/데이터 생태계가 가장 풍부        |
| LLM      | OpenAI API (GPT-4o)           | 한국어 자소서 품질이 좋음            |
| 임베딩   | OpenAI text-embedding-3-small | 가성비 좋은 임베딩 모델              |
| 벡터DB   | Supabase (pgvector)           | PostgreSQL 기반, 무료 티어 있음      |
| CLI      | Typer                         | Python CLI 프레임워크 중 가장 직관적 |
| PDF 파싱 | PyMuPDF (fitz)                | 빠르고 정확한 PDF 텍스트 추출        |

---

## 데이터

### 합격 자소서 (data.txt)

- 39건의 합격 자소서, IT 직군 위주 (2022~2025)
- 질문-답변 단위로 청킹하여 RAG에 활용
- 각 chunk에는 메타데이터 포함: 회사명, 직무, 질문 유형

### 이력서 (사용자 입력 → Supabase 저장)

- 입력 방식: 텍스트 직접 붙여넣기 / .txt / .md / PDF
- LLM으로 파싱하여 구조화: 프로젝트 경험, 기술 스택, 경력, 수상/자격증 등
- 파싱 결과를 Supabase에 저장 (원본 + 구조화 데이터 + 임베딩)
- 저장된 이력서는 다음 사용 시 재선택 가능
- 이력서 업데이트 시 덮어쓰기 지원

### 채용공고 (사용자 입력)

- 텍스트 직접 붙여넣기 (URL 스크래핑 안 함 — IP 밴 위험)
- LLM으로 요구 역량, 우대사항, 직무 키워드 추출

---

## 파일 구조

```
cover-letter/
├── data/
│   └── data.txt              # 합격 자소서 원본
├── src/
│   ├── __init__.py
│   ├── cli.py                # CLI 진입점 (Typer)
│   ├── embedder.py           # data.txt → 질문-답변 청킹 → 임베딩 → Supabase 저장
│   ├── parser.py             # 이력서 파싱 (텍스트/파일/PDF → 구조화 → Supabase 저장/조회)
│   ├── analyzer.py           # 채용공고 텍스트 → 요구 역량 추출
│   ├── retriever.py          # Supabase 벡터 유사도 검색
│   ├── generator.py          # RAG 결과 + 이력서 + 채용공고 → 자소서 생성
│   └── evaluator.py          # LLM-as-a-Judge 평가 + 재생성 판단
├── .env                      # OPENAI_API_KEY, SUPABASE_URL, SUPABASE_KEY
├── pyproject.toml
├── PLAN.md
└── README.md
```

---

## 구현 순서

### Phase 1: 기반 세팅

1. conda 환경 생성 + 의존성 설치
2. Supabase 프로젝트 생성 + pgvector 확장 활성화 + documents 테이블 생성

### Phase 2: 데이터 파이프라인

3. `embedder.py` — data.txt 파싱 → 질문-답변 단위 청킹 → 메타데이터 추출 → 임베딩 → Supabase 저장
4. 임베딩 결과 검증 (유사도 검색 테스트)

### Phase 3: 입력 처리

5. `parser.py` — 이력서 파싱 + Supabase 저장/조회
   - 신규: 파싱 → 구조화 → 임베딩 → Supabase 저장
   - 재사용: 저장된 이력서 목록 조회 → 선택
6. `analyzer.py` — 채용공고 텍스트에서 요구 역량/키워드 추출

### Phase 4: RAG + 생성

7. `retriever.py` — 질문 유형 + 요구 역량으로 유사 합격 자소서 검색
8. `generator.py` — 검색 결과 + 이력서 정보 + 채용공고 분석 → 자소서 답변 생성

### Phase 5: 평가 + 재생성

9. `evaluator.py` — LLM-as-a-Judge 평가 시스템
   - 채용공고 기반 페르소나 백그라운드 동적 생성
   - 3그룹 × 3명 = 9명 독립 평가 (asyncio 병렬)
   - 그룹별 평균 → 종합 서류 통과 확률 산출

   | 그룹          | 인원 | 평가 관점                             | 동적 백그라운드 예시               |
   | ------------- | ---- | ------------------------------------- | ---------------------------------- |
   | HR 인사담당자 | 3명  | 문체, 글 구조, 질문 의도 부합, 글자수 | 대기업/중견/스타트업 인사팀 경력자 |
   | 현업 팀장     | 3명  | 기술 매칭, 경험 구체성, 직무 이해도   | 채용공고 직무에 맞는 IT 현업 리더  |
   | 채용 리더     | 3명  | 성장 가능성, 지원동기 진정성, 차별화  | CTO/VP/임원급 의사결정자           |

10. 캘리브레이션 — 합격 자소서 100개 Q&A로 평가관 신뢰도 사전 검증
11. 재생성 로직 — 9명의 피드백을 종합 반영하여 개선된 답변 자동 생성

### Phase 6: CLI 연결

11. `cli.py` — 전체 파이프라인을 하나의 명령어로 연결
12. E2E 테스트 — 전체 흐름 검증

---

## MVP 입출력 예시

### 입력

```
자소서 질문: 팀워크를 발휘해 공동 목표 달성에 기여한 경험을 서술하세요. (600자)

채용공고: (복붙한 채용공고 텍스트)

이력서: (복붙 또는 파일 경로)
```

### 출력

```
[생성된 자소서]
(합격 자소서 톤과 구조를 참고하여,
채용공고 요구 역량에 맞게,
본인 이력서의 실제 경험을 녹여서 작성된
자연스러운 자소서 답변)

[평가 결과] (9명 평가 종합)

서류 통과 확률: 78%

┌ HR 인사담당자 (3명 평균)
│  문체 자연스러움: 8.0/10
│  글 구조: 7.7/10
│  질문 의도 부합: 8.3/10
│
├ 현업 팀장 (3명 평균)
│  기술 역량 매칭: 7.3/10
│  경험 구체성: 7.0/10
│  직무 이해도: 7.7/10
│
└ 채용 리더 (3명 평균)
   성장 가능성: 8.0/10
   지원동기 진정성: 7.3/10
   차별화 포인트: 7.0/10

주요 피드백:
- "클라우드 경험이 채용공고 핵심 요구인데 반영이 부족" (현업 팀장)
- "성과를 수치로 표현하면 구체성이 올라감" (HR)
- "지원동기에서 회사 특성 언급이 더 필요" (채용 리더)

재생성 하시겠습니까? (Y/n)
```

---

## 핵심 원칙

- **사람이 쓴 것처럼** — AI 특유의 나열식/요약식 톤을 피하고, 합격 자소서의 자연스러운 문체를 학습
- **내 경험 기반** — 허구가 아닌, 이력서에 있는 실제 경험을 재구성
- **채용공고 맞춤** — 기업이 원하는 역량에 경험을 매칭
